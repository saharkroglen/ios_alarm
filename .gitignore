# OS junk
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/
*.iml

# Dart / Flutter
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
build/
coverage/

# Keep this tracked (used by Flutter tooling)
# (Do NOT add .metadata to .gitignore)
# .metadata

# Android
android/.gradle/
android/captures/
android/local.properties
android/key.properties
android/app/*.keystore
android/app/*.jks
# Build outputs are under build/, but these are fine too:
# android/app/debug
# android/app/profile
# android/app/release

# iOS
ios/Flutter/.last_build_id
ios/Flutter/ephemeral/
ios/.symlinks/
ios/Pods/
ios/Flutter/Generated.xcconfig
**/xcuserdata/

# (Recommended: commit Podfile.lock for iOS builds)
# Remove the next line if you choose to track it:
# ios/Podfile.lock

# macOS
macos/Flutter/ephemeral/
macos/Pods/
**/xcuserdata/
# macos/Podfile.lock  # same guidance as iOS

# Linux
linux/flutter/ephemeral/

# Windows
windows/flutter/ephemeral/

# Web
# (build artifacts already ignored via build/)

# Optional: FVM (if you use it)
.fvm/
!.fvm/fvm_config.json

# Optional: SwiftPM (if you add native code)
.swiftpm/
